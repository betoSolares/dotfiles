#!/bin/sh
# Get the current volume

[ "$(amixer -c 1 sget Master | awk -F"[][]" '/dB/ { print $6 }')" == "off" ] && exit
volume=$(amixer -c 1 sget Master | awk -F"[][]" '/dB/ { print $2 }')
printf "%s" "$volume"
