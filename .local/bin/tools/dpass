#! /bin/sh

. "${HOME}/.cache/wal/colors.sh"

if [ -z "$1" ]; then
  caller="$(ps -o comm= -p $(ps -o ppid= -p $$))"
  prompt="${1:-[$caller]}"
else
  prompt="$1"
fi

dmenu -p "$prompt" -nf $color1 -nb $color1 -sf $color1 -sb $color0 <&-
